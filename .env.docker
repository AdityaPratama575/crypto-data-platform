# Override environment khusus container Docker.
# File ini dipakai bersama .env pada docker-compose.

# MySQL berjalan di host Windows; dari container gunakan hostname ini.
MYSQL_HOST=host.docker.internal

# Samakan UID container Airflow dengan UID user host agar volume mount writable.
AIRFLOW_UID=1000
